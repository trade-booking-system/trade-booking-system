version: '3.8'

services:
  redis:
    image: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
  main:
    build: ./python/api
    ports:
      - "8000:8000"
    depends_on:
      - redis
volumes:
  redis_data: